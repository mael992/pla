name: Deploy to production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Install cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/

      - name: Setup SSH config
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cat >> ~/.ssh/config << EOF
          Host ssh.planex26.com
            HostName ssh.planex26.com
            User roro
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking no
            ProxyCommand cloudflared access ssh --hostname %h
          EOF

      # Le code vit sur l'hote (bind-monte dans le conteneur) ; seules les
      # commandes PHP tournent dans le conteneur web du stack PlanEx.
      - name: 🚀 Deploy
        run: |
          ssh ssh.planex26.com "
            cd /srv/planex &&
            git -C app fetch origin main &&
            git -C app reset --hard origin/main &&
            docker compose exec -T -u 1000:1000 -e COMPOSER_HOME=/tmp/composer web \
              composer install --no-dev --optimize-autoloader --no-interaction &&
            docker compose exec -T web php artisan migrate --force &&
            (docker compose exec -T web php artisan storage:link --force || true) &&
            docker compose exec -T web php artisan config:cache &&
            docker compose exec -T web php artisan route:cache &&
            docker compose exec -T web php artisan view:cache &&
            docker compose restart scheduler
          "

      - name: ✅ Verifier que le site repond
        run: |
          for i in 1 2 3 4 5; do
            CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 20 https://planex26.com/) || CODE=000
            [ "$CODE" = "200" ] && echo "Site en ligne (HTTP 200)" && exit 0
            echo "Tentative $i : HTTP $CODE"
            sleep 10
          done
          echo "Le site ne repond pas correctement apres deploiement"
          exit 1
